import { BorderDownDependOnPublishDateDirective } from './border-down-depend-on-publish-date.directive';

describe('BorderDownDependOnPublishDateDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderDownDependOnPublishDateDirective();
    expect(directive).toBeTruthy();
  });
});
